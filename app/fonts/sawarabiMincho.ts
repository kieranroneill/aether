import { Sawarabi_Mincho } from 'next/font/google';

const font = Sawarabi_Mincho({
  display: 'swap',
  subsets: ['latin'],
  variable: '--font-sawarabi-mincho',
  weight: '400',
});

export default font;
